"use strict"

class DAOAvisos {
    constructor(pool) {
        this.pool = pool;
    }

    createAviso(aviso){
        
    }

    getAvisos(user, callback){
        if(user.rol === "Usuario"){

        } else{
            
        }
    }
}

module.exports = {
    DAOAvisos
};
